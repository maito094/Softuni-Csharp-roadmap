using System;

namespace SoftUni_Cs_Basics_Lecture_2
{
    class Program
    {
        static void Main(string[] args)
        {
            #region Intro
            //int numInt1 = 3;
            //int numInt2 = 2;

            //double numD1 = 3;
            //double numD2 = 2;

            //Console.WriteLine(numD1/numInt2);   // When Dividing ONLY INT numbers The result is in INT, and the Float part is dismissed. If Numerator or Denumerator is of type Double the Return Result is in Double with the Float part
            //Console.WriteLine(1.0* numInt1 / numInt2);   // When multiply with 1.0 the result is converted to Double


            //Console.WriteLine(1 + 1 + "4" + 2 + 1); // Everything after the String it Will Concatenate it all.
            #endregion

            #region Operator
            // Operators
            /*
                Работи за : числа, дати, стрингове и други сравними типове
                ==
                !=
                >=
                >
                <=
                <
            / Връща TRUE или FALSE
             */
            //   Console.WriteLine("softuni" == "softuni"); 


            //bool isPositive = 5 > 0;  // is Positive като задаване на въпрос.
            //Console.WriteLine(isPositive);
            //bool isTheSame = "Pesho" == "Pesho ";
            //Console.WriteLine($"Is the name same: {isTheSame}");
            #endregion

            //double money; /// Money Живее глобално!!!! когато е декларирана тука

            //if (true)
            //{
            //    double money; // Ако е Монеу е декларирана в някой Блок м/у къдрави скоби на IF/ELSE/Method, то тя живее само там и не може да бъде достъпена другаде!!!
            //}
            //else
            //{
            //    double money;
            //}
        }
    }
}
